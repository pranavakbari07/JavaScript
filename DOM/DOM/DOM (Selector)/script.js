// DOM Targeting

// // Tag Name :-
// let heading = document.getElementsByTagName("h1")
// // it always return a HTML Collection (Array).
// console.log(heading);


// // Id :-
// let heading = document.getElementById("head")
// // it always return a single elements matching the given id.
// console.log(heading);


// // Class Name :-
// let heading = document.getElementsByClassName("hello")
// // it always return a HTML Collection (Array).
// console.log(heading);


// // Query Selector (Class) :-
// let heading = document.querySelector(".hello")
// // it always return a single elements matching the given id (Return First element ya End element).
// console.log(heading);


// // Query Selector (id) :-
// let heading = document.querySelector("#head")
// // it always return a single elements matching the given id.
// console.log(heading);


// // Query Selector (Tag Name) :-
// let heading = document.querySelector("h1")
// // it always return a single elements matching the given id (Return First element ya End element).
// console.log(heading);


// // Query Selector All(Class) :-
// let heading = document.querySelectorAll(".hello")
// // it always return a HTML Collection (Array).
// console.log(heading);


// // Query Selector All(id) :-
// let heading = document.querySelectorAll("#head")
// // it always return a HTML Collection (Array).
// console.log(heading);


// // Query Selector All(Tag Name) :-
let heading = document.querySelectorAll("h1")
// // it always return a HTML Collection (Array).
console.log(heading);